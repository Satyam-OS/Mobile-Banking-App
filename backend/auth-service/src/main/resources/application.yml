server:
  port: ${PORT:8081}

spring:
  application:
    name: auth-service

  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000
      connection-timeout: 30000
      max-lifetime: 60000
      keepalive-time: 25000
      connection-test-query: SELECT 1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

account:
  service:
    url: ${ACCOUNT_SERVICE_URL}

jwt:
  secret: ${JWT_SECRET}

# Shared secret for internal service-to-service calls (no JWT needed).
# Set INTERNAL_SERVICE_SECRET env var to the same value on both
# auth-service and transaction-service Render deployments.
internal:
  service:
    secret: ${INTERNAL_SERVICE_SECRET:INTERNAL_SECRET_CHANGE_ME}

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  security:
    enabled: false
